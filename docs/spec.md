# 仕様書 兼 基本設計（当日埋める）

> このファイルは Claude Code と Codex の両方が読む「何を作るか」の唯一の正となる。
>
> ## 当日の進め方
> 1. テーマが発表されたら、Claude とのチャットに「**テーマ ＋ 制約 ＋（あれば）アイデアの種**」を貼る。
> 2. Claude が下の順で詰める: ① 課題と一言価値 → ② MVP(必ず動かす1機能)の確定 → ③ デモシナリオを先に決める → ④ 画面/データ/API → ⑤ 作業分担。
> 3. このファイルを埋めて、そのまま Claude Code / Codex に渡す。
>
> **鉄則: MVP は1つ。デモで映らない機能は書かない。**

---

## 1. 仕様

### お題 / テーマ
（当日貼る）

### 解決する課題 / 誰のため
-

### 一言で何か（エレベーターピッチ）
-

### MVP — デモで必ず動かす "1つ" の機能
-

### スコープ外（やらないこと）※ここを埋めるのが一番大事
-
-

### デモシナリオ（審査員に見せる 30〜60秒の流れ）
1.
2.
3.

---

## 2. 基本設計

### 画面 / ルート（Next.js App Router）
| ルート | 役割 | 担当 |
|---|---|---|
| `/` |  |  |

### データの形（型 / 永続化）
- 永続化の要否:（基本は不要。必要なら メモリ → localStorage → 軽量 KV）
- 主要な型:
  ```ts
  // 例
  ```

### 外部サービス / API
- 使う LLM / AI API:
- サーバー処理の置き場: `app/api/.../route.ts`
- キー管理: `.env.local` + Vercel 環境変数（コミット禁止）

### コンポーネント分割（ざっくり）
-

### 状態管理
- 方針: まず Server Component / `useState`。ライブラリは入れない。

### 作業分担マッピング
| 領域 | 担当 | 主なファイル |
|---|---|---|
| ロジック/データ/API | にゃんこ2 (Claude Code) | `lib/`, `app/api/` |
| UI/デザイン/動画/磨き込み | 相方 (Codex) | `components/`, スタイル |

---

## 3. タイムボックス（約3時間の目安）

| 経過 | やること |
|---|---|
| 0:00–0:15 | テーマ → Claude とのチャットで仕様 ＆ MVP 確定（上を埋める） |
| 0:15–0:30 | scaffold-app で空箱を作り、Vercel に Hello World をデプロイ確認 |
| 0:30–2:30 | MVP 実装（にゃんこ2 = ロジック/API、相方 = UI）。こまめに push ＆ デプロイ確認 |
| 2:30–2:45 | デモシナリオ通りに通し確認 ＋ 見た目の最終磨き |
| 2:45–3:00 | 予備 / バグ潰し、紹介・発表準備 |
