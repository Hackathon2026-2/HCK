# 仕様書 兼 基本設計（当日埋める）

> このファイルは Claude Code と Codex の両方が読む「何を作るか」の唯一の正。
>
> ## プロダクト方針（全議題共通・確定）
> どんなテーマでも、**3分で審査員に印象を残す「体感型」**を作る。
> - 核は「**体を動かす・声を出す**」など、見ているだけでなく**体で感じられる**インタラクション。MVP はこの体感を1つ成立させることに全振りする。
> - 機能の数より、1つの体感の気持ちよさ。動く URL 前提。
>
> ## 発表構成（3分・確定）
> | 時間 | 内容 |
> |---|---|
> | 0:00–1:00 | アプリ説明（**説明動画**を使う。動画は相方が制作） |
> | 1:00–2:00 | 実際にライブでプレイし、体感を見せる |
> | 2:00–3:00 | Q&A |
>
> ## アセット方針（確定）
> - 音楽: **Suno** / 効果音: **ElevenLabs** / 説明動画: **相方が制作**
> - すべて**当日その場で生成**する（テーマ依存なので事前生成しない）。基本は各Web UIで生成 → ファイルを `public/audio/` に置いて再生（API連携は時間が無ければしない）。
> - AIの活用ポイントは、アプリがLLMを呼ばなくても Suno/ElevenLabs の生成物で成立する。runtime で LLM API を使うかはテーマ次第で当日判断。
>
> ## 当日の進め方
> 1. テーマ発表 → Claude とのチャットに「テーマ＋制約＋アイデアの種」を貼る。
> 2. 詰め順: ① 誰のどんな課題 → ② **その課題を「体感」でどう解くか** → ③ 体感の核(MVP)1つ → ④ 1-1-1 のデモに落ちるか逆算 → ⑤ 画面/データ/API → ⑥ 分担。
> 3. このファイルを埋めて Claude Code / Codex に渡す。
>
> **鉄則: MVP は体感1つ。デモで映らない機能は書かない。**

---

## 1. 仕様

### お題 / テーマ
（当日貼る）

### 解決する課題 / 誰のため
-

### 体感の核（このアプリで何を体で感じさせるか）
- 入力（体 / 声 / 動き 等）:
- 反応（音 / 映像 / フィードバック 等）:

### 一言で何か（エレベーターピッチ）
-

### MVP — デモで必ず動かす "体感1つ"
-

### スコープ外（やらないこと）※ここを埋めるのが一番大事
-
-

### デモシナリオ（1-1-1 に沿って）
- 説明（0:00–1:00、動画で見せる要点）:
- ライブプレイ（1:00–2:00、審査員に体感させる流れ）:
- Q&A 想定質問:

---

## 2. 基本設計

### 実演デバイス（先に決める）
- どの端末で体感を見せるか: PC（webカメラ / マイク） or スマホ（モーションセンサー / マイク）
- オンライン round なので、**音はブラウザのタブ音声共有で審査員に届ける**前提（共有時に「タブの音声を共有」をオン）。

### 画面 / ルート（Next.js App Router）
| ルート | 役割 | 担当 |
|---|---|---|
| `/` | 体感のメイン画面（スタートボタンで開始） |  |

### 入力・センサー / 音
- 使う Web API（例: マイク=getUserMedia / 動き=DeviceMotion / 再生=Web Audio）:
- 音声ファイル置き場: `public/audio/`（Suno=BGM、ElevenLabs=SFX）

### データの形（型 / 永続化）
- 永続化の要否:（基本は不要。必要なら メモリ → localStorage）
- 主要な型:
  ```ts
  // 例
  ```

### 外部サービス / API
- LLM/AI API（使う場合のみ）: 当日判断。サーバー処理は `app/api/.../route.ts`、キーは `.env.local` + Vercel 環境変数（コミット禁止）
- 音楽=Suno / 効果音=ElevenLabs（基本は WebUI で生成しファイル配置）

### コンポーネント分割（ざっくり）
-

### 状態管理
- まず Server Component / `useState`。ライブラリは入れない。

### 作業分担マッピング
| 領域 | 担当 | 主なファイル / 成果物 |
|---|---|---|
| ロジック / 体感 / 音の制御 / API | にゃんこ2 (Claude Code) | `lib/`, `app/api/`, 音再生制御 |
| UI / デザイン | 相方 (Codex) | `components/`, スタイル |
| 説明動画 / 音声アセット生成 | 相方 | 動画、Suno / ElevenLabs の音源 |

> 説明動画はアプリの完成を待たず、コンセプトから並行制作できる（ライブプレイで実物を見せるので、動画は概念の説明でよい）。**コンセプトを早く固める**ほど、相方が動画・音源に充てられる時間が増える。

---

## 3. タイムボックス（約3時間の目安）

| 経過 | にゃんこ2 (Claude Code) | 相方 (Codex) |
|---|---|---|
| 0:00–0:15 | テーマ→チャットで体感MVP確定（上を埋める） | 同席して方針合意 |
| 0:15–0:30 | scaffold-app で空箱 → Vercel に表示確認 | UI 方針・画面ラフ |
| 0:30–1:45 | 体感の核を実装（入力→反応→音）。こまめに push ＆ デプロイ確認 | 画面UIを軽く実装 → コンセプト確定後、説明動画の制作に着手 |
| 1:45–2:15 | 体感を仕上げ、**デモ用に動く状態を固定** | **説明動画 ＋ 音源(Suno/ElevenLabs)を並行制作** |
| 2:15–2:45 | バグ潰し・予備 | 動画仕上げ・音源差し込み・UI微調整 |
| 2:45–3:00 | 通し練習（1-1-1）、デモ環境の最終確認 | 通し練習（1-1-1） |

> 最大の山は **「1:00–2:00 のライブプレイ用に、体感MVPが実機で動くこと」**。説明動画はアプリと独立に作れるので、ボトルネックはあくまで体感の実装。
